import { SxProps, Theme } from '@mui/material';

export const gridItem: SxProps<Theme> = {
  p: 3,
};

export const generalBox: SxProps<Theme> = {
  display: 'flex',
  py: 3,
};

export const avatarContainer: SxProps<Theme> = {
  display: 'flex',
  alignItems: 'center',
};

export const animatedHeader: SxProps<Theme> = {
  display: 'flex',
};
