import { SxProps, Theme } from '@mui/material';

export const formBox: SxProps<Theme> = {
  display: 'flex',
  width: 1,
};

export const formColumnBox: SxProps<Theme> = {
  display: 'flex',
  flexDirection: 'column',
  width: 1,
  height: 200,
  mr: 1,
};

export const middleTextField: SxProps<Theme> = {
  mt: 2,
  mb: '13px',
};

export const textArea: SxProps<Theme> = {
  mt: 2,
};
